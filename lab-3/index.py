import pygame
from tetris import TetrisGame


def main():
    # pygame санг эхлүүлэх
    # pygame.init() энд бичигдэх ёстой

    # дэлгэцийн өргөн, өндөр тохируулах
    # pygame.display.set_mode ашиглана

    # window-ийн гарчиг (title) тохируулах
    # pygame.display.set_caption ашиглана

    # FPS хянах clock объект үүсгэх
    # pygame.time.Clock ашиглана

    # TetrisGame классын объект үүсгэх
    # дэлгэцийг параметрээр дамжуулна

    # тоглоомын үндсэн loop эхлүүлэх
    # game.run() дуудна
    pass


if __name__ == "__main__":
    # програм main функцээс эхэлнэ
    main()
